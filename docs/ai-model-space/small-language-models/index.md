# Small Language Models

Compact LMs (often < 1B parameters) that trade peak capability for lower latency/cost and easier deployment.

## Notes

- [Build an SLM from scratch](notes/build-slm-from-scratch-tinystories.md) — TinyStories + GPT-2 tokenizer + nanoGPT-style pipeline
