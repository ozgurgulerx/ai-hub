# Model Space

This module groups model families and alternative architectures in one place.

## Subsections

- Reasoning LLMs: `reasoning-models/`
- Small language models (SLMs): `small-language-models/`
- Multimodal models: `multi-modal-models/`
- Diffusion models: `diffusion-models/`
- State space models (SSMs): `state-space-models/`
- World models (planning in latent space): `world-models/`
- Training & scaling (pre-training, post-training, compute): `training-and-scaling/`
