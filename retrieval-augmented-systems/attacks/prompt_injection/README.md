# Prompt Injection (attacks)

Add attack cases and helpers here.

Suggested structure:

- `direct/`: system override, policy bypass, jailbreak strings
- `indirect/`: injections embedded in retrieved docs, HTML, PDFs, emails

The Day001 harness lives in `eval/harness/run_attack_pack.py`.

