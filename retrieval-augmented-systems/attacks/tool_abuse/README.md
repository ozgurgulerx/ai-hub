# Tool Abuse (attacks)

Add scenarios that coerce the model into:

- calling tools with unauthorized arguments (IDs, amounts, destinations)
- escalating privileges by role confusion (“I am admin/support”)
- chaining tool calls to reach side effects

Day001 tool: `get_customer_record(customer_id)`.

